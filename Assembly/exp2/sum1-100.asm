DATAS SEGMENT
DATAS ENDS

STACKS SEGMENT
STACKS ENDS

CODES SEGMENT
    ASSUME CS:CODES,DS:DATAS,SS:STACKS
START:
    MOV AX,DATAS
    MOV DS,AX
    XOR BX,BX
    XOR AX,AX
    MOV CX,100
L1:
    INC BX
    ADD AX,BX
    LOOP L1
    MOV AH,4CH
    INT 21H
CODES ENDS
    END START
