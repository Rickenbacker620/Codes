DATAS SEGMENT
    BUF DB 20, 34, 1, 88, 6, 9, 30, 7, 25, 67
    SUM DW ?
    MAX DB ?
DATAS ENDS

STACKS SEGMENT
STACKS ENDS

CODES SEGMENT
    ASSUME CS:CODES,DS:DATAS,SS:STACKS
START:
    MOV AX,DATAS
    MOV DS,AX
    XOR AX,AX
    XOR BX,BX
    XOR DX,DX
NEXT:
    CMP BX,10
    JE  FINISH
    MOV CL,[BUF+BX]
    INC BX
    ADD AX,CX
    CMP CL,DL
    JA UPDATE
    JMP NEXT
UPDATE:
    MOV DL,CL
    JMP NEXT
FINISH:
    MOV MAX,DL
    MOV SUM,AX
    MOV AH,4CH
    INT 21H

CODES ENDS
    END START