DATAS SEGMENT
    DA1 DW 3F28H
    DA2 DW ?
DATAS ENDS

STACKS SEGMENT
STACKS ENDS

CODES SEGMENT
    ASSUME CS:CODES,DS:DATAS,SS:STACKS
START:
    MOV AX,DATAS
    MOV DS,AX
    XOR AX,AX
    MOV CX,16
    MOV BX,DA1

JUDGE:
    ROL BX,1
    ADC AX,0
    LOOP JUDGE

FINISH:
    MOV DA2,AX
    MOV AH,4CH
    INT 21H

CODES ENDS
    END START