#pragma once
#include <ctime>
#include <vector>
#include <algorithm>
#include <iostream>

using namespace std;

void shuffle(vector<int> &a);
void swap(int &a, int &b);
bool check(vector<int> &nums);
void show(vector<int> &nums);
